amends "pkl:test"

import "../datasource.pkl"

examples {
    ["DataSource Config is compiling"] {
        new datasource.Config {
            username = "test"
            password = "password"
            url = "jdbc://host:port/database"
        }
    }
}
