amends "pkl:test"

import "../redis.pkl"

examples {
    ["Redis Config is compiling"] {
        new redis.Config {
            username = "test"
            password = "password"
            url = "host1:5656"
        }
    }
}
